   <p>
         <label Roles:
                <input type="checkbox" name ="roles"
                th:each ="role: ${roles}"
                th:text ="${role.getRoleName()}"
                th:value="${role.getId()}"
                th:field="roles"
                />

         </label>
     </p